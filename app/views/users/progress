<%- model_class = User -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>

<% @completed.each do |m_id, projects| %>
  <% manual = @manuals.find{ |m| m.id == m_id } %></br></br>
  <h3><%= manual.name %></h3>
  <div>
  <% projects.each do |p, speech| %>
    <%= p.name.ljust(20) + ": " + speech.title %></br>
  <% end %>
  </div>
  <%= "Progress: " + projects.count.to_s + "/" + manual.projects.count.to_s %>
<% end %>