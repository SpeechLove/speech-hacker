<h1> Calendar </h1>

<div class="span6" id="calendar"></div>

<div class="span6 list-wrapper" id="meeting-list">
  <div>

<!--       <th>Meeting</th>
      <th>Start time</th>
      <th>Location</th>
      <% if current_user %>
        <th>Attending?</th>
        <th>Meeting Role</th>
      <% end %>
      <th>
        <% if current_user && current_user.admin? %>
          Edit
        <% end %>
      </th>
 -->
  </div>
  <div>
    <% @meetings.each do |meeting| %>
    <p>
      <%= link_to meeting.meeting_date.to_s, meeting_path(meeting) %> |
      <%= meeting.meeting_time %> |

        <%= meeting.location %> |

      <% if current_user %> 

<!--           <%= current_user.attending?(meeting) ? "Yes" : "No" %> -->
          <%= current_user.meeting_role(meeting).title if current_user.responded_to?(meeting) %>
            <% if current_user && current_user.admin? %>
              <%= link_to "Edit", edit_meeting_path(meeting) %>
            <% end %>
      <% end %>
    </p>
    <% end %>
  </div>
</div>
<div class="span6 list-wrapper hide" id="meeting-info"> 
  blah blah 

  blah
  blah

</div>
  


