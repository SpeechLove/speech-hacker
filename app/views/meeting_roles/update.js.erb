$("td.role_title").filter(function(index) {return $(this).text() == '<%= meeting_role_title %>'}).text("<%= meeting_role_params[:title] %>");
$("td.role_description").filter(function(index) {return $(this).text() == '<%= meeting_role_title %>'}).text("<%= meeting_role_params[:description] %>");
$("form.edit_meeting_role").remove();
$('#new_role_button').show();
